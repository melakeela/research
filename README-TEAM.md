# MelaKeela research team pack

Drop these into `melakeela/research`, preserving paths:

```
.claude/agents/            eight subagent definitions
00-CONTROLLER/PROGRAMME.md operating structure, gates, batch sequence
04-AUDITS/validate-registers.py   mechanical enforcement
```

The agents are drafts written outside the repository. Each must be
reconciled against CLAUDE.md and METHODOLOGY-CONSTITUTION.md before
it is committed as authoritative — see the bootstrap prompt.
