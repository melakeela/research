# Prompt-pack inventory

**Written:** 2026-09-07
**Subjects:** the six files committed unchanged to `00-CONTROLLER/prompt-pack/`
**Revised:** 2026-09-07, on the arrival of `MELAKEELAWHOMADETHEPAST.md`
**Status of this document:** derived analysis of six `INHERITED-UNVERIFIED`
sources. It establishes what each file specifies, how the five relate to each
other and to `00-CONTROLLER/METHODOLOGY-CONSTITUTION.md`, and where they
conflict with the controller this repository currently runs under. It promotes
nothing. No claim in any of the five files acquires evidentiary standing by
being inventoried here.

---

## 1. The six files

Five arrived together; `MELAKEELAWHOMADETHEPAST.md` followed and is the fifth
companion running-list Version 12 named. It is different in kind from the other
five — see §1.3.

| File | Dated | Lines | Kind | Governs |
|---|---|---|---|---|
| `MELAKEELACLAUDECORRECTIVECONTROLLERv2.md` | undated; self-dated *after* the pack | 590 | Amendment to the whole programme | Method, inheritance, the 89-item backlog, R20–R21, persistent files, owner decisions |
| `MELAKEELALANGUAGERESEARCHPROMPTS.md` | 5 Sept 2026 | 288 | Packet definitions | **R1–R19**, the shared research contract, the challenge prompt, Prompt 0 |
| `MELAKEELACLAUDEFULLSEQUENCE.md` | 5 Sept 2026 | 332 | Workflow | The nine-stage audit → research → review → release → build sequence |
| `MELAKEELACLAUDELIVESITEAUDITPROMPT.md` | 5 Sept 2026 | 96 | Single prompt | Prompt A, the live public-site audit that opens the programme |
| `MELAKEELACLAUDESAYNEXTRESEARCHPROMPT.md` | 5 Sept 2026 | 152 | Single prompt | The stateful one-chat variant: stages S00–S22 |
| `MELAKEELAWHOMADETHEPAST.md` | 5 Sept 2026 | 409 | **Research foundation** | Ancient subaltern history and intellectual provenance; the contribution/transmission data model; features F1–F8 |

Committed byte-for-byte identical to the uploads (md5 verified at commit).

### 1.1 Filenames

The five are committed under the owner's upload filenames. Every reference to
this pack elsewhere in the repository and in the site-review running list uses
**hyphenated** names — `MELA-KEELA-CLAUDE-FULL-SEQUENCE.md`,
`MELA-KEELA-LANGUAGE-RESEARCH-PROMPTS.md`,
`MELA-KEELA-CLAUDE-LIVE-SITE-AUDIT-PROMPT.md`,
`MELA-KEELA-CLAUDE-SAY-NEXT-RESEARCH-PROMPT.md`. The files also cross-reference
each other by the hyphenated names internally. The instruction was to preserve
filenames, so the unhyphenated forms stand; the correspondence is:

| Referenced as | Committed as |
|---|---|
| `MELA-KEELA-CLAUDE-FULL-SEQUENCE.md` | `MELAKEELACLAUDEFULLSEQUENCE.md` |
| `MELA-KEELA-LANGUAGE-RESEARCH-PROMPTS.md` | `MELAKEELALANGUAGERESEARCHPROMPTS.md` |
| `MELA-KEELA-CLAUDE-LIVE-SITE-AUDIT-PROMPT.md` | `MELAKEELACLAUDELIVESITEAUDITPROMPT.md` |
| `MELA-KEELA-CLAUDE-SAY-NEXT-RESEARCH-PROMPT.md` | `MELAKEELACLAUDESAYNEXTRESEARCHPROMPT.md` |
| `MELA-KEELA-WHO-MADE-THE-PAST.md` | `MELAKEELAWHOMADETHEPAST.md` |
| — (not named in the running list) | `MELAKEELACLAUDECORRECTIVECONTROLLERv2.md` |

### 1.2 Completeness against the running list

`RUNNING-LIST-RECONCILIATION.md` §"Bearing on D-014" records five companion
files named in running-list Version 12. **All five are now present.**

| Named in Version 12 | Present |
|---|---|
| `MELA-KEELA-CLAUDE-FULL-SEQUENCE.md` | yes |
| `MELA-KEELA-LANGUAGE-RESEARCH-PROMPTS.md` | yes |
| `MELA-KEELA-CLAUDE-LIVE-SITE-AUDIT-PROMPT.md` | yes |
| `MELA-KEELA-CLAUDE-SAY-NEXT-RESEARCH-PROMPT.md` | yes |
| `MELA-KEELA-WHO-MADE-THE-PAST.md` | yes — arrived 2026-09-07 |

`MELAKEELACLAUDECORRECTIVECONTROLLERv2.md` is a sixth file the running list does
not name. It post-dates the other four dated files: its own first lines are "Use
after the previously supplied MelaKeela Claude prompt-pack ZIP" and "Do not
begin the next original stage until this file has been processed."

Who Made the Past is a required input at three points —
`MELAKEELACLAUDEFULLSEQUENCE.md` "What you need" item 5, Stage 2 Project
Knowledge, and the say-next prompt's governing-documents list — and R8 is
instructed to "cross-link the documented contribution model in Who Made the
Past? rather than creating a separate incompatible schema." That instruction is
now executable.

### 1.3 One of the six is not a prompt

Five of the six are instructions to an agent. `MELAKEELAWHOMADETHEPAST.md` is a
**research foundation**: it makes historical and historiographical assertions,
cites roughly thirty sources with live URLs, and states its own access limits
source group by source group in §15. That difference has consequences the other
five do not carry.

- **Its content enters the claim register, not only the controller.** Every
  assertion in it is `INHERITED-UNVERIFIED` under the inheritance rule,
  including the ones it presents as findings. Its §15 source guide is unusually
  candid — it distinguishes "full text" from publisher abstracts and
  institutional self-descriptions, and says outright that museum accounts "are
  not independent evaluations of institutional success" — but candour is not
  retrieval. Promotion still requires a logged access-ledger event.
- **Several of its citations are dependent by its own admission.** Publisher
  descriptions for Pollock, Trautmann and Soneji; a review standing in for the
  Bronkhorst debate; an abstract for Morrison; institutional self-accounts for
  Pitt Rivers and Wereldmuseum. These are `02-SOURCES/dependency.csv` entries
  when the sources are opened, not independent confirmations.
- **Register intake is separate work and has not been done.** As with the two
  site-review running lists, the document is committed and analysed here;
  writing its claim rows into `03-REGISTERS/inherited-claims.csv` (369 rows,
  all from `01-INHERITED/claude-project-handoff.md`) is queued, not done.

---

## 2. What each file specifies

### 2.1 `MELAKEELACLAUDECORRECTIVECONTROLLERv2.md`

Ten parts. It is the source text that `METHODOLOGY-CONSTITUTION.md` expands
(§3 below), but it is not a subset of it: three components in it do not survive
into the constitution.

- **Part I — the source-dependency failure.** The concrete correction that
  started the whole amendment: the substrate packet excluded Witzel's
  Para-Munda framework while still taking its figures, stratigraphy and residue
  from Witzel. Names the sources to re-derive from (primary Rigvedic passages;
  Krishnamurti, Southworth, Burrow–Emeneau/DEDR; current Munda/Austroasiatic
  work; Kuiper and Witzel directly with their original contribution identified;
  recent specialist evaluations). Sets nine re-derivation questions — whether
  "no Dravidian loans in the earliest Rigvedic level" is independently
  reproducible; what "Level I" means and who defined it; the origin of any
  "30–40 secure loans" figure; the candidate list and its criteria; which
  conclusions descend from one source; what DEDR does and does not establish;
  whether any positive Munda derivation exists; what Munda population evidence
  constrains; and the eleven-way distinction among Munda, Proto-Munda,
  pre-Proto-Munda, Para-Munda, Kubhā-Vipāś, Language X and unidentified
  material. Specifies a **23-column reproducible candidate table**
  (`candidate_id` … `what_would_change_status`).
- **Part II — universal research constitution.** Governing standard, the
  fourteen-step sequence, chronology/geography gate, cross-domain boundary,
  source and standing rules, archive and power audit, operational
  decolonization / debrahminization / de-Indo-Europeanization, nationalist
  audit, negative evidence, translation, adversarial review.
- **Part III — research inheritance:** twenty-seven items that must not be
  lost, plus the seven-term `CONFIRMED … HELD` list.
- **Part IV — the complete v2 backlog, items 1–89**, enumerated in nine groups
  (Do now 1–8; Research first 9–19; Build next 20–24; Flagship experiences
  25–27; Public experience 28–32; Children and learning 33–37; Living Worlds
  38–40; Institutional credibility 41–46; Technical infrastructure 47–52; UX
  and IA 53–58; Visual transformation 59–65; Later 66–75; Do not build yet
  76–89), with an unnumbered note on content programmes.
- **Part V — twelve required persistent files** and the seventeen-column
  `BACKLOG-COVERAGE.csv` schema with fourteen permitted dispositions.
- **Part VI — R20 and R21**, plus the non-coding product/institutional stage.
- **Part VII — language-movement Artifact Atlas requirements**: eleven layers,
  per-state-change evidence fields, the click-through rule that an artifact
  must declare whether it supplies linguistic evidence, demonstrates material
  movement, provides only a contact setting, or provides none.
- **Part VIII — eight owner decisions.**
- **Part IX — nine additional final-ZIP directories** (`11-backlog-coverage/`
  … `19-one-year-operating-roadmap/`).
- **Part X — eleven immediate actions**, ending "READY FOR OWNER REVIEW OF
  AMENDED CONTROLLER".

**Not carried into the constitution:** the 23-column candidate table; the nine
re-derivation questions; the named comparative-Dravidian source list. The
constitution keeps the *principles* of Part I (§4E's eleven distinctions,
"unknown is residual") and drops the *instruments*. Recovering those instruments
is the main substantive gain from this file.

### 2.2 `MELAKEELALANGUAGERESEARCHPROMPTS.md`

The packet definitions. Companion to running-list Version 12 §§21–24. Contains:

- **Recommended order** for R1–R19.
- **Prompt A** — pointer to the live-site audit file.
- **Prompt 0** — Claude Code repository reconciliation before build; a
  read-only pass returning a per-work-item table of production URL, branch
  paths, coverage, production/branch difference, claims needing review, assets,
  disposition and packet.
- **The shared research contract** — pasted before every R prompt. Four
  critical operations (decolonize, debrahminize, de-Indo-Europeanize, audit
  nationalist capture); required distinctions (language vs model; phoneme vs
  phone vs letter vs grapheme vs token; composition vs copy vs edition;
  similarity vs contact vs transmission; evidence absence vs demonstrated
  absence; first attestation vs origin); an **archive-power note** and a
  **narrative audit** on every major conclusion; and a ten-part packet
  specification (answer, claim ledger, source register, chronology, comparison
  table, disposition manifest, public copy, interaction brief, correction list,
  work remaining).
- **R1–R19** in full (§4 below).
- **The challenge prompt** — applied to each packet before consolidation.
- **The Claude Code implementation prompt** — after repository reconciliation.
- **Operating controls** — one dated live-site audit, one repository
  reconciliation, one claim/source register, one rights register, one
  correction history across all packets; effort recorded per case with founder
  time distinguished from cash.

### 2.3 `MELAKEELACLAUDEFULLSEQUENCE.md`

The workflow. Its stated purpose is to answer whether the programme opens with
a live-site audit, and it answers yes:

```text
Claude Chat live-site audit → bounded research packets → evidence challenge
→ provisional content release → Claude Code repository reconciliation
→ final release reconciliation → build on a branch → preview → deployment
```

Nine stages: S1 audit; S2 one Claude Project; S3 nineteen bounded research
stages; S4 the uniform packet ZIP layout and required claim fields; S5 challenge
and revision; S6 provisional release; S7 read-only repository reconciliation;
S8 authorized release; S9 build, test and preview. It carries the run-order
table for R1–R19, the dependency graph, the required claim-row fields, the
seven relationship types, and a closing list of ten claims that must not be
published as settled.

### 2.4 `MELAKEELACLAUDELIVESITEAUDITPROMPT.md`

Prompt A in full. A read-only coverage and research-needs audit of
`melakeela.com` starting at `/`, `/enter`, `/research-index`, Explore, Exhibits,
Evidence/Ledger, Search, and any reachable sitemap. Defines the six coverage
classifications (`CONFIRMED COVERAGE | PARTIAL COVERAGE | PASSING MENTION |
CONFLICTING COVERAGE | NOT FOUND IN PUBLIC AUDIT | INACCESSIBLE/UNKNOWN`), the
per-page record fields, the `programme-coverage.csv` and `current-claim-risks.csv`
schemas, and the six-way split of `research-priorities.md` (corrections /
extensions / new investigations / shared features / research holds / probable
duplicates). It produces the `research-inputs/R#-current-public-coverage.md`
files that every R packet is supposed to receive as input.

Two rules in it match the repository's own: a route that cannot be inspected is
`INACCESSIBLE/UNKNOWN`, never absent; and a page is not commissioned merely
because its proposed title is absent.

### 2.5 `MELAKEELACLAUDESAYNEXTRESEARCHPROMPT.md`

The one-conversation variant of the same programme. The owner pastes it once
and replies **next**; the controller executes exactly one stage per turn.
Stage queue S00 (live-site audit) → S01–S19 (the nineteen packets in the run
order) → S20 cross-packet adversarial review → S21 final content architecture
→ S22 final master ZIP and handoff. Adds a research stance, a ten-point
mandatory method, its **own** eleven persistent records, the seven relationship
types, thirteen programme-specific special controls, and the S22 deliverable
tree.

### 2.6 `MELAKEELAWHOMADETHEPAST.md`

Sixteen sections developing a supplied sixteen-section proposal, with the
institutional question "Who produced knowledge, who carried it, what survived,
who could authorize it, and who eventually received credit?"

- **§1–§3 the programme and its foundations.** Five governing commitments, and
  the central distinction — **symmetry of method, not symmetry of
  responsibility**: "A colonial institution's documented seizure and a
  community's imperfect recollection are not equivalent because both require
  scrutiny. Equally, sympathy for a marginalized claim does not establish its
  chronology or transmission route." Guha, Spivak, Chakrabarty, Trouillot and
  Hartman are used for particular methodological arguments, with Ambedkar and
  Uma Chakravarti insisted on as a primary-text strand rather than precursors
  in someone else's genealogy. It requires "a positive account of making" so
  that subaltern people "must not appear only when they suffer, disappear or
  are acted upon."
- **§2 corrections to the supplied proposal.** A ten-row adopt/revise table.
  Its strongest formulation: "Knowledge can be collectively made, unequally
  preserved, creatively transformed and unfairly credited—all at once."
- **§4 historical resolution.** Rejects "the further back we go, the less we
  can see" as too categorical; replaces it with six dimensions (temporal,
  spatial, material, linguistic, social, transmission) each carrying a display
  rule, plus an **absence protocol**.
- **§5 how survival compounds.** Four mechanisms — non-recording, non-survival,
  exclusion from recognition, documented suppression or destruction — and the
  rule against collapsing them: "Calling every absence destruction invents an
  event. Calling documented destruction 'natural loss' conceals an event."
  Vedic recitation is the counterexample to any oral-equals-marginal
  assumption.
- **§6 comparison without civilizations.** Every comparison must declare
  whether it concerns linguistic genealogy, documented borrowing, institutional
  function or later classification. Jones 1786, the *Yavanajātaka*, Pāṇini
  against the *Technē Grammatikē*, Cicero, Babylonian methods in Greek
  astronomy, Ḥunayn ibn Isḥāq.
- **§7 two documentary pilots.** *Hortus Malabaricus* and the Florentine Codex,
  chosen because contribution and transmission are inspectable in surviving
  documents — proposed as validation of the provenance model "before a much
  less resolvable ancient genealogy is attempted."
- **§8 Sanskritic authority and empire.** Pollock, Srinivas's Sanskritization
  used narrowly, Greater Magadha as an investigation candidate, Morrison on
  colonial inheritances in South Asian archaeology, Trautmann on admiration
  coexisting with racial hierarchy. A seven-axis comparative table.
- **§9 Aryan racialization as a branching history, not an inevitable chain.**
  Separates an ancient self-designation, a philological classification, an
  ethnological claim and a racial-political category.
- **§10 the shared data model.** Custody of a physical record and transmission
  of an idea kept as two histories. Eight record types; evidence roles;
  **"Bridges are claims"**; the argument against a single flat status scale
  (§5, `PP-10`); and "Do not compute a civilizational ownership percentage."
- **§11 eight priority features**, F1–F8, each with visitor question,
  interaction, research dependency and acceptance criterion.
- **§12–§13 coverage architecture and existing coverage.** The twenty-room wing
  reduced to five collections; a supplied-section-to-treatment table; and
  integration actions against a *local audit snapshot* that it explicitly
  refuses to treat as a verified inventory (`PP-13`).
- **§14 research operations.** Case dossier before feature; a claim lifecycle;
  review matched to claim type; community authority with documented scope and
  no unlimited veto; "Pay for expertise and participation." Pitt Rivers and
  Wereldmuseum as precedents, "not proof that either institution has completed
  decolonization."
- **§15 source guide and seven research lanes still requiring original work.**

Where it sits in the constitution: this is the fullest statement of **domain V**
(ancient subalterns and intellectual provenance) anywhere in the material, and
it carries most of §12's product-specification subject list in usable form. Its
§9 belongs to no domain at all (§4.1).

---

## 3. How the six relate

### 3.1 To each other

```text
                    MELAKEELACLAUDECORRECTIVECONTROLLERv2.md
                    (later amendment; supersedes nothing but
                     re-governs everything below it)
                                   ▲
                                   │ amends
  ┌────────────────────────────────┴────────────────────────────────┐
  │                                                                 │
FULLSEQUENCE.md ──── names ────► LIVESITEAUDITPROMPT.md   (Stage 1 / Prompt A)
  │  the workflow                 LANGUAGERESEARCHPROMPTS.md (Stage 3 / R1–R19)
  │                               SAYNEXTRESEARCHPROMPT.md   (Stage 3 variant)
  │                               WHOMADETHEPAST.md          (standing input to
  │                                                           every stage; R8's
  │                                                           named schema)
  └── also requires: the master work list (D-038)
```

The fifth companion is not a stage. All three workflow files list it among the
governing documents held in Project Knowledge for the whole programme, not
attached to one packet — and `LANGUAGERESEARCHPROMPTS.md` names ancient
subaltern history and intellectual provenance as "core research programmes" in
the shared contract every packet receives. It also supplies two things the pack
asks for but does not define: the **evidence-role vocabulary** behind the
shared contract's `evidence_role` field (`PP-4`), and the contribution model R8
is told to cross-link rather than duplicate.

- `FULLSEQUENCE` is the spine. It names the other three 5 September files as
  its inputs and states the order they run in.
- `LIVESITEAUDITPROMPT` is Stage 1 of `FULLSEQUENCE` and S00 of `SAYNEXT`. Its
  output feeds one `R#-current-public-coverage.md` into each packet.
- `LANGUAGERESEARCHPROMPTS` supplies the content of Stage 3 / S01–S19. Its
  "Prompt A" section is a pointer to `LIVESITEAUDITPROMPT`, not a duplicate.
- `SAYNEXT` is an *alternative execution mode* for the same stages, not
  additional work. `FULLSEQUENCE` §3 says so directly: one chat per packet is
  "the lower-context alternative … not required for the owner's 'say next'
  sequence."
- `CORRECTIVECONTROLLERv2` sits above all four. It does not replace the run
  order; it adds R20/R21, the 89-item reconciliation, the persistent-file set
  and the method, and it requires the earlier pack to be read first.

The three files that state a run order for R1–R19 — `LANGUAGERESEARCHPROMPTS`
"Recommended order", `FULLSEQUENCE` Stage 3 table, `SAYNEXT` S01–S19 — **agree
exactly**: R1, R9, R16, R3, R10, R12, R17, R2, R4, R11, R18, R13, R19, R14, R5,
R6, R8, R15, R7. No reconciliation is needed between them.

### 3.2 To `METHODOLOGY-CONSTITUTION.md`

`METHODOLOGY-CONSTITUTION.md` is an **expansion of
`MELAKEELACLAUDECORRECTIVECONTROLLERv2.md`**, not an independent document. Same
opening move ("amends the prompt-pack ZIP already supplied"; "do not discard or
restart completed work"), same seven immediate tasks, same closing string
("READY FOR OWNER REVIEW OF AMENDED CONTROLLER"). Part-to-section mapping:

| Controller v2 | Constitution | Relationship |
|---|---|---|
| Part I (Witzel/source dependency) | §1 (the failure), §4E, §4F | **Abstracted.** Failure pattern generalized; nine questions, source list and 23-column candidate table dropped |
| Part II governing standard | §2 | Expanded; contradiction list made explicit |
| Part II required sequence 1–14 | §5 Steps 1–14 | Same fourteen steps, one paragraph each in the constitution |
| Part II chronology/geography gate, cross-domain boundary, source rules, archive audit, operational de-* , nationalist audit | §5 Steps 2/3/5/6/7/10/11, §8 | Redistributed into the steps and the adversarial tests |
| Part II negative evidence | §6 | Same eight absence types |
| Part II translation | §7 | Same required fields, same inherited-category list |
| Part II adversarial review | §8 | Same two challenges |
| Part III (27 inheritance items + 7 dispositions) | §3 + §4 A–V | **Restructured** into 22 lettered domains, each with a named trap; item 27 (Atlas) moves to §13 |
| Part IV (items 1–89, enumerated) | §10 | **Requirement kept, list dropped.** §10 declares the backlog binding and specifies the CSV, but does not enumerate the 89 items |
| Part V (12 files + CSV schema + dispositions) | §9 | Identical set, identical schema, identical fourteen dispositions |
| Part VI (R20, R21, product stage) | §11, §12 | Same, expanded |
| Part VII (Atlas) | §13 | Same eleven layers, same evidence fields |
| Part VIII (8 owner decisions) | §14 | Same eight |
| Part IX (9 ZIP directories) | §15 | Same nine |
| Part X (11 actions) | §16 | Twelve actions; adds "read the complete 89-item backlog" |

Two consequences follow. First, **nothing in the prompt-pack overturns the
constitution** — the constitution is the later and fuller statement of the same
amendment. Second, **the prompt-pack restores three things the constitution
dropped**: the enumerated 89 backlog items (§5.1 below), the substrate candidate
table and re-derivation questions, and the entire R1–R19 layer, which the
constitution never contained at all — §11 adds R20 and R21 to a queue whose
first nineteen entries were, until now, absent from this repository.

---

## 4. Which of R1–R19 they define

All nineteen are defined, in full, in **`MELAKEELALANGUAGERESEARCHPROMPTS.md`**.
`FULLSEQUENCE` and `SAYNEXT` give one-line purposes and the run order but no
definitions; `LIVESITEAUDITPROMPT` supplies each packet's public-coverage input;
`CORRECTIVECONTROLLERv2` defines none of R1–R19 and adds R20–R21.
`WHOMADETHEPAST` defines none either, but is a named input to **R8** and bears
substantively on **R2, R4, R5, R6, R7, R11 and R13** — §6 supplies the
Pāṇini/*Technē* comparison and the Yavanajātaka and Babylonian–Greek
transmission cases, §4 the Indus resolution problem, §10 the provenance model
R13's museum audit needs.

`Run` is the position in the agreed order. `LS` lists the work-list IDs the
packet prompt itself names. `Domain` is a **proposed** mapping onto constitution
§4 A–V, derived here, not stated in any source file; overlap is partial.

| R | Title | Run | LS named | Depends on | Domain (proposed) |
|---|---|---|---|---|---|
| R1 | What Tamil Reveals About AI | 1 | LS-01, 02, 21 | — | *none* |
| R2 | Before Pāṇini and Inside the Grammar | 8 | LS-03, 04, 09 | R3, R9 | C (partial) |
| R3 | Did Later Rules Reshape Earlier Vedic Speech? | 4 | LS-05, 06, 15 | R9, R16 | A |
| R4 | Before Tolkāppiyam and Meaning Has a Landscape | 9 | LS-07, 08, 09, 11, 19 | — | D |
| R5 | Who Mapped the Mouth? Writing, Sound and Dates | 15 | LS-10, 11, 12, 13 | R6 | *none* |
| R6 | Multiple Sciences of Language | 16 | LS-09, 10, 14, 15, 16 | R2, R4 | N (partial) |
| R7 | Contact, Meaning and Directionality | 19 | LS-17, 18, 19 | R2, R4, R5, R15 | P, Q |
| R8 | When Knowledge Becomes Sacred Authority | 17 | LS-20 | R2 (source IDs) | U, V (partial) |
| R9 | What Is Inherited, Indic and South Asian in Rigvedic Sound? | 2 | — | — | B, C |
| R10 | The Rigveda as a Layered Social and Ritual Archive | 5 | — | — | A, I, J |
| R11 | Substrate Vocabulary and Grammatical Ontologies | 10 | — | — | E, F, N |
| R12 | The Oxus Interface and Indo-Iranian Contact Before the Split | 6 | — | — | G |
| R13 | Stones, Routes and the Political Economy of Exchange | 12 | — | — | R, S |
| R14 | The Language That Changed as It Moved | 14 | — | R3, R9, R11, R12, R16, R18 | B, O, P |
| R15 | Before Alexander: Contact Corridors and Intellectual Transmission | 18 | — | R7 (coordinate) | Q |
| R16 | Proto-Dravidian Sibilants, Tamil Script and Borrowed Sound | 3 | — | R9 | D |
| R17 | The Same World, Two Responses | 7 | — | R10, R12 | B, H |
| R18 | Brahui, North Dravidian and the Languages Left on the Hills | 11 | — | R11, R12 | M |
| R19 | Before Oil: Hormuz, Corridors and Coercive Control | 13 | — | R13 | S, T |

Stated dependency graph (`FULLSEQUENCE` Stage 3):

```text
R9 → R16 → R3 → R2
R10 + R12 → R17
R11 + R12 → R18
R13 → R19
R3 + R9 + R11 + R12 + R16 + R18 → R14
R2 + R4 + R5 + R15 → R7
```

### 4.1 Coverage against the constitution's domains

Nineteen packets and twenty-two domains are **not congruent**, in both
directions.

- **Domains no R1–R19 packet covers:** **K** (Indus writing and institutional
  discontinuity) and **L** (Avestan writing) — both belong to R20; **U**
  (sacrifice, renunciation, appropriation) — R21, with R8 touching only its
  attribution half. This is consistent: R20 and R21 were added by
  `CORRECTIVECONTROLLERv2` precisely because the nineteen left those gaps.
- **Packets no domain covers:** **R1** (modern model behaviour on Tamil),
  **R5** (writing systems and Brāhmī dating), and largely **R2** and **R6**
  (the comparative history of grammatical science). Constitution §4 A–V is an
  ancient-evidence inheritance; four packets sit outside it. They are not
  therefore out of scope — running-list Version 12 carries them as LS items —
  but they have no domain-level trap statement, which is what §4 exists to
  supply. Writing those four traps is outstanding work.
- **A subject with neither packet nor domain:** the **racialization of "Aryan"**.
  `WHOMADETHEPAST` §9 makes it a full programme — an ancient self-designation, a
  philological classification, an ethnological claim and a racial-political
  category, kept apart as "a branching history, not an inevitable chain" — and
  §2 states the discipline both ways: document the racialization "through
  specific actors and institutions", and "reject an inevitable
  Sanskrit-to-Holocaust chain." Constitution §4 has no domain for it and no
  packet covers it. `CLAUDE.md`'s governing principle requires the record to
  stay capable of contradicting colonial scholarship, which is adjacent but not
  the same subject. This is a fifth missing trap statement, and the one whose
  absence carries the most risk: §9 also sets a learning requirement against
  interfaces that "ask children to sort human beings into racial types" or
  "turn persecution into an aesthetic spectacle."
- **LS mapping is incomplete.** Only R1–R8 name LS IDs, and only LS-01–LS-21 —
  the Version 9 set. R9–R19 were added in running-list Versions 10–12 and their
  LS-22–LS-51, COR-01–COR-18 and RV-01/RV-02 assignments live in the running
  list, not in the prompt-pack. Anyone tracing a packet to its coverage items
  for R9–R19 must go through `01-INHERITED/site-review/MELAKEELASITEREVIEWRUNNINGLIST.md`.

---

## 5. Conflicts with the current controller

Numbered `PP-`. Where `00-CONTROLLER/CONTROLLER-RECONCILIATION.md` has already
resolved the same conflict arriving from the constitution, the existing
`C-` resolution is cited and extended rather than re-argued.

`PP-1` to `PP-9` come from the first five files. `PP-10` to `PP-14` come from
`MELAKEELAWHOMADETHEPAST.md` and were added on 2026-09-07; `PP-14` records
convergences rather than conflicts.

### PP-1 — The Version 11 step reversal: the audit gate is missing from the governing controller

This is the flagged conflict, and it is not the one the flag anticipated.

`01-INHERITED/site-review/RUNNING-LIST-RECONCILIATION.md` records that
running-list Version 11 reversed the programme's first step: Version 10 opened
with a Claude Code repository inventory, Version 12 opens with a Claude Chat
live public-site audit, and "anything built on Version 10's step order is
working from a superseded sequence."

**The four 5 September files do not re-propose Version 10's order.** They are
the reversal's own instruments. `FULLSEQUENCE` states it in as many words —
"that live audit can replace the earlier idea that Claude Code must inventory
the repository before any research begins" — and puts repository reconciliation
at Stage 7, after reviewed packets, exactly as Version 12's line 1098 requires.
`LIVESITEAUDITPROMPT` is Prompt A; `SAYNEXT` makes it S00;
`LANGUAGERESEARCHPROMPTS` opens its recommended order with it. On the direction
of the reversal, the pack and the running list agree.

**The conflict is with the constitution, and it is a deletion rather than a
reversal.** `METHODOLOGY-CONSTITUTION.md` contains no live-site audit stage at
all:

- the string `melakeela.com` occurs **zero times** in it;
- §16's twelve immediate actions contain no audit step;
- site-checking survives only as §5 **Step 13**, one step inside the fourteen
  applied to each individual investigation.

So the governing document takes neither side of the Version 11 decision. It
drops the standalone gate that the decision produced and folds the site check
into per-investigation method. `CORRECTIVECONTROLLERv2` does the same — Part X's
eleven actions have no audit stage, and its Part II step 13 is the
per-investigation check. Since `CORRECTIVECONTROLLERv2` post-dates the pack and
the constitution expands it, the demotion is the *later* position, and it is
undocumented: no change log, no reasoning, nothing in
`CONTROLLER-RECONCILIATION.md`, which does not mention the audit at all.

Two further facts make this live rather than academic:

1. **The reversal was about which agent goes first, and there is now one
   agent.** `CONTROLLER-RECONCILIATION.md` §4 rules the Claude Chat / Claude
   Code split "obsolete as a division of labour here. This repository *is*
   Claude Code, and it produces the research." Version 11's question —
   repository-inventory-first or live-site-audit-first — was posed between two
   agents that no longer exist as separate roles. With one agent the question
   becomes whether a live public-site audit precedes the first research
   programme at all. Nothing in the repository answers it.
2. **This repository cannot presently run the audit.** `LIVESITEAUDITPROMPT`
   requires browsing `melakeela.com`; the site lives in `melakeela/site`, which
   this session cannot read, and outbound retrieval goes through the egress
   proxy. Whether the audit is a gate or a step, its feasibility is a separate
   question already half-raised as D-009 and in D-014's closing paragraph.

**Recorded, not resolved.** Reinstating an audit-first gate would be a
programme-ordering decision; so would ratifying the demotion. Both belong to the
owner. Raised as a new decision (§7).

### PP-2 — Stop-and-wait becomes a standing rule

`SAYNEXT` makes one-stage-per-turn the operating mode: "complete exactly one
numbered stage at a time … wait for me to say **next** before advancing," ending
each stage with a checkpoint ZIP and "Ready for next."

`CLAUDE.md` runs the opposite rule: "Do not stop at 'sources identified' and
hand back a plan. Run the retrieval," and "commit and push after every
substantive unit of work, not only at the end of the task."

`CONTROLLER-RECONCILIATION.md` **C-6** already resolved the constitution's §16
stop as "a one-time gate on the controller amendment itself … not a standing
rule of work." `SAYNEXT` is the harder case, because for it the stop *is* the
architecture. **Extension of C-6:** the say-next execution mode does not govern
here. Its stage queue survives as a queue; its turn-taking protocol does not.
The pull request is the gate, and a stage that finishes mid-task is committed
and followed by the next, not handed back.

### PP-3 — Four claim-status vocabularies, none of them the register's

The repository runs one status vocabulary (`VERIFIED` … `HOLD`) with the
constitution's `CONFIRMED … HELD` recorded beside it as a disposition — the
**C-1** resolution. The pack adds three more:

| Vocabulary | Source | Kind |
|---|---|---|
| `CONFIRMED SUPPORTED PLAUSIBLE REQUIRES-VERIFICATION REVISED REJECTED HELD` | `CORRECTIVECONTROLLERv2` Part III | disposition — already mapped by C-1 |
| `supported / revise / open / reject` (+ `HELD`) | shared contract §2; `FULLSEQUENCE` claim fields | packet-level claim status |
| `attested / reconstructed / inferred / possible / disputed / rejected / unknown` | `SAYNEXT` method item 6 | **evidential type, not status** |
| `CONFIRMED COVERAGE / PARTIAL / PASSING MENTION / CONFLICTING / NOT FOUND IN PUBLIC AUDIT / INACCESSIBLE-UNKNOWN` | `LIVESITEAUDITPROMPT` | **route coverage, not claim status** |

Only the second competes with the register's `status` column. **Extension of
C-1:** `03-REGISTERS/` `status` stays the repository's seven; `supported /
revise / open / reject` maps onto it (`supported`→`PROVISIONAL` or `VERIFIED`
according to whether a retrieval is logged; `revise`→a `SUPERSEDED` row plus its
replacement; `open`→`HYPOTHESIS`; `reject`→`REJECTED`; `HELD`→`HOLD`). The third
and fourth are not statuses at all and must not be written into a `status`
column; the third belongs with the attestation gradient, the fourth in a route
inventory.

### PP-4 — The packet claim schema has no retrieval date

`FULLSEQUENCE` Stage 4 requires:

```text
claim_id,public_sentence,source_id,locator,evidence_role,inference,
counter_evidence,scope,date_or_stratum,status,affected_route
```

`CLAUDE.md` requires:

```text
claim_id,claim,status,source_id,locator,retrieval_date,supports_page,notes
```

The overlap is close — `public_sentence`≈`claim`, `affected_route`≈`supports_page`
— but the packet schema has **no `retrieval_date`**, and the repository's
inheritance rule turns on exactly that field: "Promotion out of
`INHERITED-UNVERIFIED` requires an actual retrieval event logged in
`02-SOURCES/access-ledger.csv`. Argument does not promote a claim."

**Proposed resolution:** the repository schema is authoritative and
`retrieval_date` is not optional. The packet schema's four extra columns
(`evidence_role`, `inference`, `counter_evidence`, `scope`, `date_or_stratum`)
are additions worth having and can be carried in `notes` or in added columns; a
packet claim row without a retrieval date enters at `INHERITED-UNVERIFIED`
regardless of the status the packet assigns it. This is the inheritance rule
applied to a new class of input, not a new rule.

### PP-5 — A third persistent-file scheme

`CORRECTIVECONTROLLERv2` Part V names the same twelve files as constitution §9,
already given homes by **C-3**. `SAYNEXT` names eleven *different* ones —
`MASTER-MANIFEST.json`, `PROGRAMME-STATUS.csv`, `ROUTE-DISPOSITION.csv`,
`CLAIM-LEDGER.csv`, `SOURCE-REGISTER.csv`, `CHRONOLOGY.csv`,
`RELATIONSHIPS.json`, `CORRECTIONS.md`, `OPEN-QUESTIONS.csv`,
`RIGHTS-AND-ASSETS.csv`, `SPECIALIST-REVIEW-QUEUE.csv` — and `FULLSEQUENCE`
Stage 4 names a twelfth per-packet set.

Overlaps are near-duplicates of stores that already exist or are already
assigned: `CLAIM-LEDGER`/`SOURCE-REGISTER` against `03-REGISTERS/` and
`02-SOURCES/access-ledger.csv`; `OPEN-QUESTIONS` against
`RESEARCH-QUESTION-REGISTER.csv`; `CORRECTIONS.md` against the
correction-history rule and `04-AUDITS/`; `MASTER-MANIFEST.json` against the
git history.

**Extension of C-3:** one store per subject. Three genuinely new subjects have
no home yet and should get one when work fills them —
`ROUTE-DISPOSITION.csv`, `RIGHTS-AND-ASSETS.csv` and
`SPECIALIST-REVIEW-QUEUE.csv`. The rest are aliases; do not create them.

### PP-6 — ZIP deliverables

Every one of the five files ends in an archive requirement:
`MK-live-site-audit-2026-09-05.zip`, `MK-R#-short-title-v1.zip`,
`MK-R#-short-title-v2-reviewed.zip`, `MK-release-01-provisional`,
`MK-research-checkpoint-S##.zip`, `MK-MELAKEELA-MASTER-RESEARCH-v1.zip`, plus
`CORRECTIVECONTROLLERv2` Part IX's nine additional directories.

Already resolved by `CONTROLLER-RECONCILIATION.md` §4: ZIPs are commits, the
branch is the checkpoint, "verify the ZIP opens" is a file-existence check
before the PR, and Part IX's `11-` … `19-` numbering does not survive because
this repository's `00-`–`05-` scheme means something else. **No change; the
resolution now covers five more sources.** The nine subject areas remain valid
as directories created when their work begins.

### PP-7 — R19's expiring modern layer

R19 requires independent verification, "as of the research date," of
contemporary claims about U.S.–Iran hostilities, blockade, oil flows,
Venezuela, ownership stakes, Maduro, Puerto Rico and commodity shares, with
version-pinned percentages and an explicit expiry: "the current-affairs layer
expires unless refreshed."

The repository has no mechanism for a claim that expires. Every register row is
durable; `SUPERSEDED` requires a successor, and there is no "stale" state. R19
also reaches well outside the evidence base every other packet draws on.

**Proposed resolution:** R19's ancient corridor and its modern comparison are
two units. The ancient half runs under the ordinary register rules. The modern
half needs either a dated-expiry convention or an owner decision to defer it;
`CORRECTIVECONTROLLERv2` and the constitution both keep the subject (domain T,
"Hormuz and modern comparison"), so deferring the layer is a scoping choice, not
a rejection. Raised in §7.

### PP-8 — Paid model runs

R1 specifies a reproducible experiment with "model/tokenizer versions,
settings, date and spending cap" and "do not run paid APIs without an agreed
budget"; the operating controls require per-case effort tracking with founder
time separated from cash. This is not a conflict — `CLAUDE.md` escalates
"payment or institutional access required" to the owner — but it means **R1
cannot be completed as specified without a budget decision**, and R1 is first in
the run order. Raised in §7.

### PP-9 — Non-conflicts worth adopting

Three instruments in the pack have no counterpart in the controller and no
conflict with it:

1. **The seven relationship types** — `genealogy | borrowing | transmission |
   analogy | shared_source | contact_possible | disputed` — stated identically
   in `FULLSEQUENCE` and `SAYNEXT`. `CROSS-DOMAIN-BRIDGES.csv` (constitution §9,
   C-3 home `03-REGISTERS/`) has no type vocabulary; this is the obvious one.
2. **The 23-column substrate candidate table** from `CORRECTIVECONTROLLERv2`
   Part I — the operational instrument for domains E and F, dropped by the
   constitution.
3. **The ten claims that must not be published as settled** (`FULLSEQUENCE`
   closing section) — a ready-made watch list that overlaps but does not
   duplicate `CLAUDE.md`'s "positions already settled by the owner."
4. **The evidence-role vocabulary** (`WHOMADETHEPAST` §10) — see `PP-11`.
5. **"Bridges are claims"** (§10): "Resemblance is not automatically
   transmission. Priority is not automatically influence. Shared vocabulary is
   not automatically a shared institution. Population affinity is not
   automatically language, religion or modern community identity." This is
   `CLAUDE.md`'s Step 10 and the cross-domain boundary in four sentences, and
   is the natural header for `CROSS-DOMAIN-BRIDGES.csv`.
6. **The six resolution dimensions and their display rules** (§4) — temporal,
   spatial, material, linguistic, social, transmission, each with a rule such
   as "do not let one map pin represent" findspot, production place and later
   repository. Directly usable in `04-AUDITS/` and in any object record.

### PP-10 — One status column against a four-axis argument

`WHOMADETHEPAST` §10 argues directly against the design this repository runs:

> Do not implement one flat scale reading "documented, inferred, plausible,
> contested, speculative, unknown." A claim can be well documented and
> contested. A document can exist while its interpretation is uncertain.
> Record at least **evidence basis**, **assessment of the inference**,
> **disagreement**, and **review status** separately; simplify their
> presentation in context.

`CLAUDE.md` mandates exactly one flat scale — "Every claim carries exactly one
status. No claim is unstatused" — over `VERIFIED · PROVISIONAL · HYPOTHESIS ·
INHERITED-UNVERIFIED · REJECTED · SUPERSEDED · HOLD`.

The objection has force, and it is not the same objection as `PP-3`. `PP-3` is
about competing *vocabularies* for one axis; this is the claim that one axis is
the wrong shape. The repository's seven values do conflate at least two things:
`VERIFIED` and `PROVISIONAL` grade the evidence basis, `HYPOTHESIS` grades the
inference, `HOLD` is a review state, and none of them can express "well
documented and contested" — which is the ordinary condition of most of what
this evidence base will hold. Several rows already strain against it: an
`INHERITED-UNVERIFIED` claim whose source is known to be dependent has no way
to say so in `status`.

Against that: the single column is what makes the inheritance rule
enforceable — one field decides whether a claim may be built on — and a
four-column scheme invites a row that is `documented / strong / disputed /
unreviewed` and is thereby quietly promoted without a retrieval event. §10 also
supplies the answer to its own risk in the same breath: "The record is the unit
of truth ... must not imply that entering a proposition into the ledger makes
it true."

**Not resolvable here.** Changing the register schema is the same class of
decision as `D-019` (the `object_id` column) and belongs to the owner. Raised
as **D-039**. The single `status` column stands until it is answered; the four
axes can be carried in `notes` in the meantime without a schema change.

### PP-11 — Two more vocabularies, one of which resolves PP-4

`WHOMADETHEPAST` adds two, and they are different in kind from the four in
`PP-3`.

**Evidence roles** (§10): `supports · contradicts · contextualizes · dates ·
localizes · identifies · cannot discriminate`, typing the *evidence-to-claim*
relation, with the requirement that "each relation should explain the
argumentative work performed."

This is not a competitor. It **resolves a gap named in `PP-4`**: the shared
research contract requires an `evidence_role` field in every packet claim
ledger and never defines its values. They are defined here. Adopt as the
vocabulary for that field, alongside the seven relationship types from `PP-9`,
which type claim-to-claim edges. The two are complementary and neither touches
`status`. Note especially `cannot discriminate` — a value the repository has no
way to express at present, and the honest label for evidence consistent with
two rival explanations.

**Claim lifecycle** (§14): `draft · checked · reviewed · published · revised ·
withdrawn`, with the rule that "the person who writes the public story should
not be its only scholarly verifier."

This is an editorial workflow state, not a claim status, and it presumes a
review function this repository does not yet have — the pull request and Codex
review are its current equivalent. **Resolution:** it does not enter `status`.
It is a candidate for the review column a four-axis schema would need, and so
rides on `D-039`.

### PP-12 — Two absence taxonomies, one coarser than the other

Constitution §6 types absence eight ways. `WHOMADETHEPAST` §5 types it four,
and §4 adds a five-question absence protocol. They are compatible; the four are
a coarser partition of the eight, and the mapping is clean but not total:

| §5 mechanism | Constitution §6 types |
|---|---|
| Non-recording | `NOT PRODUCED` |
| Non-survival | `NOT PRESERVED` |
| Exclusion from recognition | `NOT EXCAVATED`, `NOT PUBLISHED`, `NOT ACCESSIBLE`, `NOT RECOGNIZED` |
| Documented suppression or destruction | `DOCUMENTED DESTRUCTION` |
| — | `ABSENT DESPITE ADEQUATE SEARCH` |

**Resolution:** the constitution's eight govern; they are strictly more
specific, and collapsing four excavation, publication, access and recognition
failures into one "exclusion" is exactly the kind of blur the negative-evidence
standard exists to prevent. The eighth has no §5 counterpart, and that is the
important one — it is the only type that licenses an argument from absence at
all, and §5's four have no way to say it.

What §4's protocol adds and should be kept: it requires "plausible alternative
explanations" alongside the typed absence, and it states the asymmetry the
constitution implies but does not spell out — "An absent inscription in an
unexcavated landscape has different force from a systematically absent feature
in a well-specified, well-sampled corpus. Neither should become automatic proof
of the preferred history."

### PP-13 — A third voice on the audit question, and it does not settle it

`WHOMADETHEPAST` §13 works from what it calls "a local audit snapshot" and
refuses to treat it as authoritative:

> It is not a verified inventory of the current production site or current
> development branch. The following are integration candidates, not claims that
> a route is currently missing or that an observed issue remains live.
> ... Routes known from previous reports but absent from the local snapshot
> require reconciliation against the actual branch and deployment. Do not
> recreate them merely because they were not in one working folder.

This bears on **D-035** and cuts across it rather than for either side. It
agrees with `LIVESITEAUDITPROMPT` that an uninspectable route is unknown rather
than absent — the same rule under a different name — but it treats the
*branch*, not the live site, as what a snapshot must be reconciled against,
which is closer to Version 10's order than to Version 11's. It is best read as
a third position: neither inventory settles anything alone, and the failure
mode it names — recreating a page because one working folder lacked it — is
the same failure mode `LIVESITEAUDITPROMPT` names from the other direction.

**Recorded on D-035**, which should be answered with this paragraph in view.
It does not change the finding at `PP-1`: the constitution still has no audit
of either kind.

### PP-14 — Convergences worth recording

Four places where `WHOMADETHEPAST` states an existing rule more sharply than
the controller does, and the wording is worth keeping:

1. **On the two adversarial tests.** `CLAUDE.md` says correct both biases
   "without pretending their archival and institutional power has been equal."
   §1 puts the same rule as **symmetry of method, not symmetry of
   responsibility**, and gives it a worked case: a documented seizure and an
   imperfect recollection "are not equivalent because both require scrutiny."
2. **On source independence.** §5: "Ten modern accounts repeating one colonial
   translation are not ten independent witnesses. The ledger should expose
   shared source ancestry and distinguish independent observation from citation
   multiplication." That is `02-SOURCES/dependency.csv`'s purpose, stated as a
   display requirement rather than a bookkeeping one.
3. **Against fabricated quantities.** Three separate prohibitions — no "72%
   historical visibility" score (§4), the illustrative 100-to-1 survival funnel
   "is not historical data and must never become a factual chart" (§2), and "do
   not compute a civilizational ownership percentage" because "a denominator
   such as 'all Sanskrit knowledge' is not established" (§10). The repository
   has the principle; it does not have this list of the specific numbers that
   tempt.
4. **On the register's own standing.** §10: the record is "the unit that can be
   inspected and corrected", and entering a proposition into the ledger does
   not make it true. Worth quoting into any future statement of what
   `03-REGISTERS/` is for.


## 6. What is still missing

| Missing | Needed for | Recorded at |
|---|---|---|
| The full "MelaKeela.com v2 — Master Research, Product & Institutional Backlog" document behind the 89 titles | the per-item detail `BACKLOG-COVERAGE.csv` columns need beyond a title | D-014, amended |
| A live-site route inventory | `current_site_coverage`; the audit's coverage columns; Step 13 | D-009, D-014 |
| The master work list itself, as distinct from the site-review running list | every file in the pack names it as a required attachment | D-038 |

`MELA-KEELA-WHO-MADE-THE-PAST.md` was on this list until 2026-09-07 and has
since arrived; see §1.2.

On the fourth: all five files require "the current master work list" as an
input, and `LANGUAGERESEARCHPROMPTS` describes itself as "Companion to master
work list Version 12, sections 21–24." Running-list Version 12 is committed at
`01-INHERITED/site-review/MELAKEELASITEREVIEWRUNNINGLIST.md` and carries §§21–24
and the LS/COR/RV series. It is most likely the same document under a different
name, but the pack never equates them and this inventory does not either.

---

## 7. Decisions this raises

Allocated from `09-DECISIONS/OWNER-DECISIONS.csv`; see that file for the
authoritative rows.

- **D-035 — Does a live public-site audit gate the first research programme?**
  PP-1. Reinstate the Version 11 gate, ratify the constitution's demotion of it
  to Step 13, or replace it with a repository-side route inventory.
- **D-036 — R19's modern layer.** PP-7. Run it with a dated-expiry convention,
  or defer it and keep the ancient corridor.
- **D-037 — R1's model-run budget.** PP-8. R1 is first in the run order and
  cannot be completed as specified without one.
- **D-038 — Is running-list Version 12 the "master work list"?** §6. If it is,
  say so and the pack's required inputs are complete but for Who Made the Past.
- **D-039 — Does the register keep one `status` column, or split into evidence
  basis, inference, disagreement and review?** `PP-10`. The same class of
  schema decision as D-019.
- **D-014 — amended twice.** §6 and §8; see `DECISIONS-NEEDED.md`.

`PP-13` adds material to **D-035** without changing it. `PP-11`, `PP-12` and
`PP-14` are resolved here and need no decision.

## 8. What the pack closes

`MELAKEELACLAUDECORRECTIVECONTROLLERv2.md` Part IV **enumerates all eighty-nine backlog items**,
verified 1–89 with no gaps. Constitution §10 requires a row per item but does
not list them; until now neither did anything in this repository, and D-014
recorded that writing eighty-nine rows would mean inventing them.

That is no longer true for the `backlog_id` and `title` columns, and for
several others the grouping alone determines — the "Do not build yet" group
(76–89) carries its own disposition, as do "Later" (66–75) and "Research first"
(9–19). It remains true for `current_site_coverage` and `existing_route`, which
need the live-site inventory, and for `prior_research_available`, which needs
the packets. **D-014 moves from blocked to partially satisfiable**, and the
question it now puts to the owner is narrower: is Part IV's list the
authoritative backlog, or a digest of a longer document that should still be
committed?

**`MELAKEELAWHOMADETHEPAST.md` closes the pack's other named gap.** All five
companions running-list Version 12 identified are now committed. R8's
instruction to cross-link a documented contribution model rather than invent a
schema is executable; domain V has its fullest statement; the §12
product-specification stage has F1–F8, the five collections and a data model to
work from; and the shared contract's undefined `evidence_role` field has a
vocabulary (`PP-11`).

What it does not close: its own content is `INHERITED-UNVERIFIED` and its claim
rows are not yet in `03-REGISTERS/` (§1.3), and its §9 opens a subject — the
racialization of "Aryan" — that has neither a packet nor a domain (§4.1).
